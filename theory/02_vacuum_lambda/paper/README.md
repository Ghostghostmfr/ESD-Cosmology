# Theory 02 paper notes

Second entry in the `theory/` track. Apply axioms A1-A3 to the
cosmological vacuum Lambda:

- A1 fails (vacuum has no system/spectator split).
- A2 fails (no Newtonian g of a bound mass; only de Sitter rate).
- R(u) does NOT modify Lambda.

**Positive results (5 PASS):**
- w(z) = -1 exactly, all z
- f_DE(z) = 1 across [0, 1100]
- Omega_Lambda_ESD = 0.68417 (Planck 0.6842, 2.9e-5 frac err)
- Friedmann sum closes
- rho_Lambda(z=0) = rho_Lambda(z_recomb) (no running)

**Honest negatives (2 PASS, important):**
- Omega_Lambda NOT a phi-power lock (best 5.76%)
- Lambda/M_Pl^4 NOT a phi-power lock (best 5%/15% in two conventions)
- ESD does NOT solve the cosmological-constant problem

**Methodological catch:**
Initial log-space scan suggested a phi-lock at 0.04% — but this is
below the trivial-coincidence floor (0.087%) set by phi-grid spacing
log10(phi) = 0.209. Tightened to linear-space 5-sig-fig test: no
real lock under either reduced or non-reduced M_Pl convention.

**Falsifiers:**
- w_0 != -1 or w_a != 0 at 5sigma (DESI Y3/Y5 + Euclid Y1)
- Detected time-variation of Lambda
